# Baseline Runs

- `baseline1/baseline1_primary/`: canonical Baseline 1 result snapshot.
- `baseline3/`: place for Baseline 3 run outputs (`baseline3_<timestamp>`).

Each run folder should contain:
- `manifest.json`
- `results.json`
