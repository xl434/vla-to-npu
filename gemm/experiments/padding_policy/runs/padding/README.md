# Padding Runs

- `padding_only_primary/`: canonical padding-only benchmark dataset.
- `analysis_primary/`: canonical transition DB + threshold fit from padding sweeps.

Recommended for new analysis:
- write to `analysis_latest/` and keep `analysis_primary/` as stable reference.
