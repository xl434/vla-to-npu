# Claude Rules for This Project

## Git

- **Never run `git commit`** without explicit user permission.
- When asked to stage changes, only run `git add <files>` and stop there.
- Leave committing entirely to the user.
